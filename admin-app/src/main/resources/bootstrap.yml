eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9001/eureka/
server:
  port: 9011
spring:
  application:
    name: admin-app
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:9006/
feign:
  hystrix:
    enabled: true
management:
  security:
    enabled: false


